services:
    feedback.comment:
        class: FeedbackBundle\Services\CommentService
        arguments: [ "%feedback.routing%", "submitted" ]
        calls:
            - [ setDoctrine, ["@doctrine"]]
            - [ setLogger, ["@monolog.logger"]]
            - [ setRouter, ["@router"]]
