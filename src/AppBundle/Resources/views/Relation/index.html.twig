{% extends 'base.html.twig' %}

 {% block pageheader %} 
    <h1>Relation List</h1>
    <p class="count">
        Displaying {{ relations|length }} relations of {{ relations.getTotalItemCount }} total.
    </p>
 {% endblock %} 

{% block body %}

        <div class='btn-toolbar pull-right'>
        <div class='btn-group'>
            <a href="{{ path('relation_new') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-plus"></span> New
            </a>
            <a href="{{ path('relation_search') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-search"></span> Search
            </a>
        </div>
    </div>
        
    <table class="table table-bordered table-condensed table-hover table-striped">
        <thead>
            <tr>
                <th>Name</th>            </tr>
        </thead>
        <tbody>
        {% for relation in relations %}
            <tr>
                <td>
                    <a href="{{ path('relation_show', { 'id': relation.id }) }}">
                        {{ relation.name }}
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
        
    <div class="navigation">
        {{ knp_pagination_render(relations) }}
    </div>

{% endblock %}
