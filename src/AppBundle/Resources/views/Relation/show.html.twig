{% extends 'base.html.twig' %}

 {% block pageheader %} 
    <h1>Relation</h1>
 {% endblock %} 

{% block body %}

    <div class="btn-toolbar">
    <div class="btn-group pull-right">
        <a href="{{ path('relation_index') }}" class="btn btn-default">
            <span class="glyphicon glyphicon-arrow-left"></span> Back
        </a>
        <a href="{{ path('relation_edit', { 'id': relation.id }) }}" class="btn btn-default">
            <span class='glyphicon glyphicon-edit'></span> Edit
        </a>
        <a href="{{ path('relation_delete', { 'id': relation.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this Relation?">
            <span class='glyphicon glyphicon-remove'></span> Delete
        </a>
    </div>
</div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
            <tr>
                <th>Name</th>
            <td>{{ relation.name }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
