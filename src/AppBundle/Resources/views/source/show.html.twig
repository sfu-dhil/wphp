{% extends 'base.html.twig' %}

{% block pageheader %} 
    <h1>Source</h1>
{% endblock %} 

{% block body %}

    <div class="btn-toolbar">
        <div class="btn-group pull-right">
            <a href="{{ path('source_index') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-arrow-left"></span> Back
            </a>
            {% if is_granted('ROLE_CONTENT_ADMIN') %}
                <a href="{{ path('source_edit', { 'id': source.id }) }}" class="btn btn-default">
                    <span class='glyphicon glyphicon-edit'></span> Edit
                </a>
                <a href="{{ path('source_delete', { 'id': source.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this Firm?">
                    <span class='glyphicon glyphicon-remove'></span> Delete
                </a>
            {% endif %}
        </div>
    </div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ source.name }}</td>
            </tr>
        </tbody>
    </table>

    {% embed 'AppBundle:title:partials/page.html.twig' with {'titles': titles} %}
    {% endembed %}

{% endblock %}
