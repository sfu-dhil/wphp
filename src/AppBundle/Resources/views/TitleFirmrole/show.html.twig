{% extends 'base.html.twig' %}

 {% block pageheader %} 
    <h1>TitleFirmrole</h1>
 {% endblock %} 

{% block body %}

    <div class="btn-toolbar">
    <div class="btn-group pull-right">
        <a href="{{ path('titlefirmrole_index') }}" class="btn btn-default">
            <span class="glyphicon glyphicon-arrow-left"></span> Back
        </a>
        <a href="{{ path('titlefirmrole_edit', { 'id': titleFirmrole.id }) }}" class="btn btn-default">
            <span class='glyphicon glyphicon-edit'></span> Edit
        </a>
        <a href="{{ path('titlefirmrole_delete', { 'id': titleFirmrole.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this TitleFirmrole?">
            <span class='glyphicon glyphicon-remove'></span> Delete
        </a>
    </div>
</div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
        </tbody>
    </table>

{% endblock %}
