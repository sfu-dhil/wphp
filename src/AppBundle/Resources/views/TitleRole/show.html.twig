{% extends 'base.html.twig' %}

 {% block pageheader %} 
    <h1>TitleRole</h1>
 {% endblock %} 

{% block body %}

    <div class="btn-toolbar">
    <div class="btn-group pull-right">
        <a href="{{ path('titlerole_index') }}" class="btn btn-default">
            <span class="glyphicon glyphicon-arrow-left"></span> Back
        </a>
        <a href="{{ path('titlerole_edit', { 'id': titleRole.id }) }}" class="btn btn-default">
            <span class='glyphicon glyphicon-edit'></span> Edit
        </a>
        <a href="{{ path('titlerole_delete', { 'id': titleRole.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this TitleRole?">
            <span class='glyphicon glyphicon-remove'></span> Delete
        </a>
    </div>
</div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
        </tbody>
    </table>

{% endblock %}
