{{ form_start(form) }}
{{ form_widget(form) }}
<div class="form-group">
    <div class="col-sm-2"></div>
    <div class="col-sm-10">
        <input type="submit" value="Save" class="btn btn-primary"/>
        {% if title_relationship.id is null %}
            <a href="{{ path('title_relationship_index') }}" class="btn btn-warning">
                Cancel
            </a>
        {% else %}
            <a href="{{ path('title_relationship_show', { 'id': title_relationship.id }) }}" class="btn btn-warning">
                Cancel
            </a>
        {% endif %}
    </div>
</div>
{{ form_end(form) }}

